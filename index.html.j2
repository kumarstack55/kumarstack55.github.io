<!-- This HTML file was generated by render_index.py. -->
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title>k10s</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/kognise/water.css@latest/dist/dark.min.css">
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
    <style type="text/css">
      <!--
      h1 {display: inline; font-size: 1.1em; font-weight: normal; }
      h1:after {content: ' : '; }
      ul {display: inline; padding-inline-start: 0px; }
      li {display: inline; }
      li:after {content: ' / '; }
      li:last-child:after {content: none; }
      /* 2020-02-23 Android+Chrome でアイコン幅50%がアイコン直後の文字と重なるのでpaddingする */
      span.icons {font-size: 0.5rem; padding-left: 0.5rem; padding-right: 0.5rem }
      -->
    </style>
  </head>
  <body>
    {%- for item in data %}
    <h1 id="{{ item.heading }}">#{{ item.heading }}</h1>
    <ul>
    {%- for li in item.list_items %}
      <li>{% if li.icons is defined %}<span class="icons">{%- for icon in li.icons %}<i class="{{ icon }}"></i>{% endfor %}</span>{% endif -%}<a href="{{ li.url }}" target="_blank" rel="noopener noreferrer">{{ li.name }}</a></li>
    {%- endfor %}
    </ul>
    {%- endfor %}
  </body>
</html>
