# Vim

## ファイルの末尾の改行を保存しない。

```vim
:set binary
:set noeol
```

## vim 起動時にコマンドを実行する。

```vim
vim -u NONE -N --noplugin -c 'qa!'
```

```vim
vim -u NONE -N --noplugin -c 'execute("normal! iabc\<esc>")' -c 'w! /tmp/a.txt' -c 'qa'
```
